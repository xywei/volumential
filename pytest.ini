[pytest]
testpaths = test
markers =
    full_accuracy: high-cost derivative accuracy tests, skipped unless --full-accuracy
    slow: aggregate runtime over roughly 30s on a CPU OpenCL backend; still run by default, deselect with -m 'not slow'
